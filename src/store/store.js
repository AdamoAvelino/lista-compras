import { stores as auth } from '../modules/auth'
import { stores as global } from '../modules/components'
import { stores as lista } from '../modules/lista'

export default {
  auth,
  global,
  lista
}
